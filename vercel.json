{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": "frontend",
  "functions": {
    "api/analyse.py": {
      "runtime": "python@3.11"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
